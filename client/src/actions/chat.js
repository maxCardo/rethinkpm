//testing socketIO connection

// import io from 'socket.io-client';

// const socket = io.connect('http://localhost:5000/')

// socket.on('test', (data) => {
//     console.log('socket test: ', data)
// })

//must import socket io to app.js for socket call to work on this page.

export function loadChats(dispatch) {
  
}